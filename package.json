{
  "name": "music-port",
  "private": true,
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "husky": "^8.0.3",
    "lerna": "^5.5.2"
  },
  "scripts": {
    "build": "lerna run build",
    "format": "lerna run --stream format",
    "lint": "lerna run --stream lint",
    "prepare": "husky install"
  }
}
